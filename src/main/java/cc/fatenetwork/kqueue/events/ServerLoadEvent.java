package cc.fatenetwork.kqueue.events;

public class ServerLoadEvent {

    /* todo
    This will be called once a server is loaded. We could also make it so there is a server and queue reload command that will re-load the configuration and reload all the queues and servers
    So once a server is laoded a broadcast could be snet through bungee telling players that a server is now online.
     */
}
